<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>لوحة تحكم الأرشيف</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>لوحة تحكم الأرشيف <span style="font-size: 0.7em;">(Replit)</span></h1>
        <a href="/admin/add" class="add-button">➕ إضافة شخصية جديدة</a>
    </header>

    <main class="dashboard">
        <% Object.keys(data).forEach(section => { %>
            <section class="section-card">
                <h2><%= section.replace('_', ' ') %> (<%= data[section].length %>)</h2>
                <ul>
                    <% data[section].forEach(item => { %>
                        <li>
                            <span><%= item.name %></span>
                            <div class="actions">
                                <a href="/admin/edit/<%= section %>/<%= item.id %>" class="edit-btn">تعديل</a>
                                <form action="/admin/delete/<%= section %>/<%= item.id %>" method="POST" style="display: inline;">
                                    <button type="submit" class="delete-btn" onclick="return confirm('هل أنت متأكد من الحذف؟')">حذف</button>
                                </form>
                            </div>
                        </li>
                    <% }); %>
                </ul>
            </section>
        <% }); %>
    </main>
</body>
</html>
